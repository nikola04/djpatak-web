import { ScriptProps } from "next/script";

export default function PlayerControlls(props: ScriptProps){
    return null //TEMP

    return <div className={`${props.className} flex bg-blue-sky z-10 shadow-md`} style={props.style}>

    </div>
}